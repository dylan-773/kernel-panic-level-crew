# Dive gate: hard

## board-architect.json
- board: APPROVE - grid [13,11] matches the brief's explicit "grid: 13x11 (explicit, given at invocation)" requirement, so there was no discretion to exercise there. slag stays at the shipped default 0.18, well clear of the ceiling difficulty.md gives: "Past 0.25 boards start failing to generate." minCost 26 follows "Roughly `2 x width` is achievable" (26 = 2x13), and minCost/minPd/parFlat (26/8/3) are identical to the worked 13x11 example in reference/schema.md, so the values are known-good rather than novel. sim.txt confirms the board actually generates cleanly at this spec across 200 seeds: `endings core 196, gridlock 2, severed 2` — no generation failures, no stuck boards. One caveat that doesn't change the verdict: the rationale claims 13x11 runs "2.9-3.1 avg rounds regardless of pressure," but the table row the Pressure Designer actually picked (13x11/oppRam7/headStart2) measures 2.3 avg rounds, matching sim.txt's `avg rounds 2.3` — the "regardless of pressure" framing oversells what the same table shows once oppRam and headStart climb. Doesn't sink the item since grid was mandated by the brief and 2.3 still clears the round floor below.

## pressure-designer.json
- pressure: APPROVE - claimed targetWinPct 32, sim.txt measured `win rate 31.5% (63/200)` with `drift -0.5` against the 32% claim — well inside the tolerance difficulty.md sets: "A dive that measures more than 15 points off its claimed target fails the simulator." oppRam 7 + headStart 2 is exactly the prescribed pairing for this difficulty: difficulty.md's named-difficulty table lists `hard | 28-40% | oppRam 6-7, headStart 2`, and 31.5%/32% sits centered in that 28-40% band, not at either extreme the gate is told to watch for. headStart was left at 2 rather than pushed to 3-4 alongside oppRam 7, so the two levers difficulty.md warns compound ("[headStart] compounds with oppRam because it shortens SIG-0's route as well as handing it ground") are not both maxed here — oppRam 7 is one below its 3-8 ceiling, headStart 2 is at the midpoint of its 0-4 range, not the ceiling. avg rounds measured 2.3, above the floor difficulty.md flags: "Under 2 is over before the player has read the board." No programs, abilities, loadout, customer, or story fields appear in the item; it's playerRam/oppRam/greed/headStart/targetWinPct only, matching the pressure contract in reference/schema.md.

Seen 2, approved 2, revised 0, noted 0.
